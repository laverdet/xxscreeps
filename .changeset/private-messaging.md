---
"xxscreeps": patch
---

Implement private messaging: send/list/index/mark-read/unread-count endpoints, live `newMessage` and read-receipt socket channels, and a `User.remove` hook to clean up a deleted user's messages.
