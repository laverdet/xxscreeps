---
"xxscreeps": patch
---

Add the `decorations` mod's catalog: yaml-authored packs shipping real asset files, checked at load against everything the client's renderer will dereference, with generated inventory previews.
