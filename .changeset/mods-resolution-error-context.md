---
"xxscreeps": patch
---

Name the failing specifier in the `SuppressedError` thrown by `config/mods.ts`, so an unresolvable mod (or provider) is reported by name instead of a bare `Module Not Found`.
