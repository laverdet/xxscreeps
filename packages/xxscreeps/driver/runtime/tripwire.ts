// We use Webpack alias configurations to replace various includes to this one, to prevent the
// backend from leaking into the frontend package.
export {};
throw new Error('An undesired file has leaked into the game runtime');
